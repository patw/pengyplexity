"""Core application logic: model client, agent loop, search, research, artifacts,
storage, auth, sharing, and images.

Importing this package is light; individual submodules import their own
dependencies (moofile, ddgs, reportlab, ...) so the offline test suite can pull
in exactly what it needs.
"""
